# Checklist rejestracji OSF (dla Pawla, po tagu spektra1-seal)

1. Konto: osf.io -> zaloguj (lub zaloz na [konto kierownika badania]).
2. Create new project: "SPEKTRA-1: Spectral signatures of correlational
   structure in LLM hidden layers (preregistered)".
3. Add component typu "Registration" -> wybierz szablon
   "Open-Ended Registration".
4. W polu opisu wklej: (a) URL repo https://github.com/pawelsokaris-sudo/spektra1
   (b) nazwe tagu: spektra1-seal (c) SHA-256 archiwum pakietu Z TAGU
   (git tag -n99 spektra1-seal | grep SHA-256) (d) krotki opis =
   docs/OPIS-DOSWIADCZENIA.md sekcje 1-3.
5. Zarejestruj z natychmiastowym upublicznieniem (bez embarga) -> OSF
   nadaje DOI i datownik.
6. DOI wpisac do README repo (commit PO pieczeci = dozwolony, bo nie
   zmienia tresci pakietu, tylko dodaje odnosnik).
